import React from "react";

import "./toggle.css";

const CustomToggle = () => {
    return (
        <div className="toggle">
            <div className="toggle-btn" />
        </div>
    );
};

export default CustomToggle;
