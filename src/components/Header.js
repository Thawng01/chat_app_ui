import React from "react";

import "./header.css";

const Header = () => {
    return (
        <div className="header">
            <h4 className="header-title">Stay connected with your friends</h4>
        </div>
    );
};

export default Header;
